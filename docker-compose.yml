version: '3.9'

services:
    web:
        build: .
        container_name: airbnb-worldwise
        restart: always
        ports:
            - '3000:3000'
        environment:
            NODE_ENV: production
            NEXT_PUBLIC_SITE_URL: http://localhost:3000
